<%- include('header.ejs'); %>

      <div class="dashboard-content">

        <div class="dashboard-grid">
          <div class="col-5">
            <div class="dashboard-panel">
              <div class="dashboard-panel-header">
                <h3>New Treatment Note</h3>
              </div>
              <div class="dashboard-panel-body">
                <form action="/doctor/treatment_save" method="post">
                  <div class="form-group">
                    <label class="form-label">Patient <span class="required">*</span></label>
                    <select class="form-control" name="treatment_p_id" required>
                      <option value="">Select Patient</option>
                      <% for(var i=0;i<data.length;i++){ %>
                      <option value="<%= data[i].p_id %>"><%= data[i].p_name %></option>
                      <% } %>
                     
                    </select>
                  </div>
                  <div class="form-group">
                    <label class="form-label">Disease / Condition <span class="required">*</span></label>
                    <input type="text" name="disease" class="form-control" placeholder="e.g. Hypertension" required />
                  </div>
                  <div class="form-group">
                    <label class="form-label">Diagnosis <span class="required">*</span></label>
                    <textarea name="treatment_diagnosis" class="form-control" placeholder="Clinical findings and diagnosis summary…" required></textarea>
                  </div>
                  <div class="form-group">
                    <label class="form-label">Treatment Notes</label>
                    <textarea name="treatment_notes" class="form-control" placeholder="Recommended treatment plan, procedures, advice…"></textarea>
                  </div>
                  <div class="form-group">
                    <label class="form-label">Medicines</label>
                    <input type="text" name="tmedicines" class="form-control" placeholder="e.g. Amlodipine 5mg, Atorvastatin 20mg" />
                    <span class="form-hint">Separate multiple medicines with commas.</span>
                  </div>
                  <div class="form-group">
                    <label class="form-label">Next Visit</label>
                    <input type="date" name="treatment_nextvisit" class="form-control" />
                  </div>
                  <div class="form-actions">
                    <button type="reset" class="btn btn-outline">Clear</button>
                    <button type="submit" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i> Save Note</button>
                  </div>
                </form>
              </div>
            </div>
          </div>

          <div class="col-7">
            <div class="dashboard-panel">
              <div class="dashboard-panel-header">
                <h3>Recent Treatment Notes</h3>
              </div>
              <div class="dashboard-panel-body no-pad">
                <div class="activity-feed" style="padding:0.5rem 1.35rem;">
                  <% for(var i=0;i<data1.length;i++){ %>
                  <div class="activity-item">
                    <div class="notification-icon success"><i class="fa-solid fa-heart-pulse"></i></div>
                    <div class="notification-body">
                      <h5><%=data1[i].p_name %></h5>
                      <p><%=data1[i].disease %> , <%=data1[i].treatment_diagnosis %> , <%=data1[i].treatment_notes %> , <%=data1[i].tmedicines %></p>
                      <span class="notif-time"><%= new Date(data1[i].treatment_nextvisit).toLocaleDateString('en-GB') %></span>
                    </div>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>
<%- include('footer.ejs'); %>
