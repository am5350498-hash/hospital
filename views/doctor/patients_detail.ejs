
<%- include('header.ejs'); %>

      <div class="dashboard-content">

        <div class="page-toolbar">
          <h2>Patient Records</h2>
          
        </div>

        <!-- Patient Detail -->
        <div class="dashboard-panel" id="patient-detail">
          <div class="dashboard-panel-header">
            <h3>Patient Details</h3>
          </div>
          <div class="dashboard-panel-body">
            <div class="user-meta" style="margin-bottom:1.5rem;">
              <div>
                <div class="name" style="font-size:1.15rem;"><%= data[0].p_name %></div>
                <div class="sub">Male · 42 yrs · O+ · Hypertension follow-up</div>
              </div>
            </div>
            <div class="form-row" style="margin-bottom:1.5rem;">
              <div>
                <label class="form-label">Phone</label>
                <p><%= data[0].p_phone %></p>
              </div>
              <div>
                <label class="form-label">Email</label>
                <p><%= data[0].p_email %></p>
              </div>
            </div>
            <div style="margin-bottom:1.5rem;">
              <label class="form-label">Address</label>
              <p><%= data[0].p_address %></p>
            </div>
            <h4 class="form-section-title">Appointment History</h4>
            <div class="activity-feed">
              <div class="activity-item">
                <div class="notification-icon success"><i class="fa-solid fa-calendar-check"></i></div>
                <div class="notification-body">
                    <% if (data1 && data1.length > 0) { %>
                  <h5>Follow-up visit — <%= data1[0].app_reason %></h5>
                  <p><%= data1[0].app_date %> · <%= data1[0].app_time %> </p>
                 <% } else { %>
            <h5>No appointment found</h5>
            <p>This patient has no appointment history.</p>
        </div>
    </div>
<% } %>
                </div>
              </div>
              <div class="activity-item">
                <div class="notification-icon"><i class="fa-solid fa-notes-medical"></i></div>
                <div class="notification-body">
                  <h5>Treatment note added — Amlodipine dosage adjusted</h5>
                  <p>May 02, 2026 · 09:15 AM</p>
                </div>
              </div>
              <div class="activity-item">
                <div class="notification-icon warning"><i class="fa-solid fa-flask"></i></div>
                <div class="notification-body">
                  <h5>Lab work ordered — Lipid panel</h5>
                  <p>Feb 10, 2026 · 11:30 AM</p>
                </div>
              </div>
            </div>
          </div>
          <div class="modal-footer" style="border-top:1px solid var(--border-light);display:flex;gap:0.75rem;padding-top:1.25rem;">
            <a href="/doctor/treatment" class="btn btn-outline"><i class="fa-solid fa-notes-medical"></i> New Treatment Note</a>
            <a href="/doctor/prescriptions" class="btn btn-primary"><i class="fa-solid fa-file-prescription"></i> New Prescription</a>
          </div>
        </div>

      </div>
    </div>
  </div>
<%- include('footer.ejs'); %>